<view class="container">
  <!-- 全局背景图 -->
  <image class="page-bg" src="{{bgUrl}}" mode="aspectFill"></image>

  <!-- 自定义返回按钮 -->
  <view class="custom-back" bindtap="goBack">
    <text class="back-icon">‹</text>
    <text class="back-text">返回</text>
  </view>

  <button class="create-btn" bindtap="createNews">新建新闻</button>

  <view wx:if="{{newsList.length === 0 && !loading}}" class="empty">暂无新闻</view>

  <view class="news-item glass-card" wx:for="{{newsList}}" wx:key="_id">
    <view class="info">
      <text class="title">{{item.title}}</text>
      <text class="meta">{{item.category}} | {{item.date}}</text>
      <!-- 草案公告显示投票统计 -->
      <view wx:if="{{item.isDraftProposal}}" class="vote-stats">
        <text class="stat">T {{item.voteStats.agreeCount || 0}}</text>
        <text class="stat">F {{item.voteStats.disagreeCount || 0}}</text>
        <text class="stat">O {{item.voteStats.abstainCount || 0}}</text>
      </view>
    </view>
    <view class="actions">
      <button size="mini" bindtap="editNews" data-id="{{item._id}}">编辑</button>
      <button size="mini" type="warn" bindtap="deleteNews" data-id="{{item._id}}">删除</button>
    </view>
  </view>
</view>