<view class="container">
  <!-- 全局背景图 -->
  <image class="page-bg" src="{{bgUrl}}" mode="aspectFill"></image>

  <!-- 背景装饰斜线 -->
  <view class="bg-diagonal"></view>

  <!-- 返回按钮 -->
  <view class="back-btn" bindtap="goBackToMenu">
    <text class="back-icon">‹</text>
    <text class="back-text">返回</text>
  </view>

  <!-- 搜索框 -->
  <view class="search-bar">
    <input 
      class="search-input" 
      placeholder="搜索活动标题或地点" 
      value="{{keyword}}" 
      bindinput="onSearchInput"
      confirm-type="search"
    />
  </view>

  <!-- 活动列表区域 -->
  <block wx:if="{{filteredList.length > 0}}">
    <view 
      class="activity-card glass-card" 
      wx:for="{{filteredList}}" 
      wx:key="_id"
      bindtap="goDetail"
      data-id="{{item._id}}">
      
      <view class="activity-cover" wx:if="{{item.coverThumb || item.cover}}">
        <image class="cover-img" src="{{item.coverThumb || item.cover}}" mode="aspectFill"></image>
      </view>

      <view class="skew-tag">{{item.type || '活动'}}</view>
      <view class="activity-title">{{item.title}}</view>
      <view class="activity-info">
        <text class="info-item">时间：{{item.startTime}}</text>
        <text class="info-item">地点：{{item.location}}</text>
      </view>
      <view class="data-block">
        <text class="data-highlight">{{item.currentParticipants}}/{{item.maxParticipants}}</text>
        <text class="data-label">已报名</text>
      </view>
    </view>

    <view class="loading-dots" wx:if="{{listLoading}}">
      <view class="dot"></view>
      <view class="dot"></view>
      <view class="dot"></view>
    </view>

    <view class="no-more" wx:if="{{!hasMore && filteredList.length > 0}}">没有更多活动了</view>
  </block>

  <!-- 空状态 -->
  <view class="empty" wx:if="{{filteredList.length === 0 && !listLoading}}">
    <text>{{keyword ? '没有找到匹配的活动' : '暂无活动，敬请期待'}}</text>
  </view>
</view>